;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list
      '(("eproject-0.3" "/home/sensorflo/.emacs.d/site-lisp/")))

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top . 0)
        (left . 0)
        (width . 91)
        (height . 54)))

